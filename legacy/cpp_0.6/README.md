# Historical C++ 0.6 laboratory core

`mld` C++ 0.6 was the active laboratory implementation before the
COMMUNITY-0.10 migration. It is preserved for source and evidence traceability.

Root CMake, active CI and repository navigation do not build this directory.
Use its local `CMakeLists.txt` only when reproducing the historical laboratory
profile in an explicitly isolated build.
