# COMMUNITY-0.10 verification

The active integration checks are the C++ CTest targets declared by the root
`CMakeLists.txt`, the Python analysis contract in
`engine/tests/life/test_community_analysis.py`, and the import/navigation
checks in `tools/`.

The supplied command logs are retained under
`provenance/incoming/cpp_0.10.0_community/verification/`; they document the
delivery environment but are not a fresh local verification.
