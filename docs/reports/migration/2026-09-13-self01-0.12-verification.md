# SELF-0.1 / 0.12 migration verification

## Input integrity

- archive: `my_life_deviation_SELF01_verified_0.12.0.zip`;
- archive SHA-256:
  `B038460398AA82FD8652B820718B721E2B73DFA4D5B979B3E770A75061B37C4`;
- ZIP files: 722;
- `SOURCE_MANIFEST.json`: 569/569 recorded source files matched their ZIP
  path, size and SHA-256 before extraction;
- `DELIVERY.json` evidence: 147/147 copied files matched their recorded size
  and SHA-256.

`SELF01_from_0.11.patch` is preserved as provenance only. It targets the
separate 0.11 archive stated by the supplier and was not applied to this
repository.

## Structural result

The active C++20 engine is SELF-0.1 / 0.12 at `engine/include/life/` and
`engine/src/life/`, with the CLI at `apps/simulate/life_main.cpp`, tests at
`engine/tests/life/`, COG reference at `reference/cognition_0.4/`, and scenario
tools under `tools/`.

The former active COMMUNITY-0.10 engine, its CLI, COG reference, tools, root
CMake and C++ workflow are preserved under `legacy/cpp_0.10/`. Existing rules,
profiles, reports and historical material remain tracked. SELF-MODEL-0.1 is
published by role under `docs/rules/self_model_0.1/`,
`docs/architecture/self_model_0.1/`, `game/profiles/self_model_0.1/` and
`docs/reports/self_model_0.1/`.

## Fresh checks performed

| Check | Result |
| --- | --- |
| `python -m unittest discover -s tools/tests -v` | 6 tests passed |
| `python tools/verify_navigation.py` | passed |
| `python tools/verify_self01_0_12_import.py` | passed; 87 mapped source files checked |
| Python syntax compilation | 11 files compiled |
| Profile JSON parse | 5 profiles valid |
| CMake source references | 28/28 active `life` sources exist |
| CTest registration inspection | 24 registrations: root plus COG reference |
| `DELIVERY.json` evidence | 147/147 files valid |
| `git diff --check origin/main...HEAD` | passed |

## Native build boundary

This Windows checkout has no `cmake`, `g++`, `clang++` or `cl` command
available. Consequently CMake configuration, C++ compilation, CTest and the
SELF behaviour scenario were not freshly run here. The supplied Linux release
and sanitizer results remain historical evidence, not local execution claims.
