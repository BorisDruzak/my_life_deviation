# COMMUNITY-0.10 migration verification

## Input integrity

- archive: `my_life_deviation_cpp_0.10.0_community.zip`;
- SHA-256: `ECA0BC3CC07027550FDF9626702DD1944D27D7588712E3524CFD49160007314D`;
- ZIP file entries: 665;
- supplied delivery manifest entries: 664, all size and SHA-256 checks passed;
- the sole unmanifested ZIP file is `DELIVERY_MANIFEST.json` itself, which
  cannot self-hash inside its own manifest.

The tracked provenance record contains the delivery manifest, source version,
archive hash, imported-role manifest and the supplied `src/persistence.cpp`.
That source file is preserved only as unbuilt evidence because the supplied
CMake target does not list it.

## Structural result

`life_core` is the active C++20 target at `engine/src/life/`, with public
headers at `engine/include/life/`, CLI at `apps/simulate/life_main.cpp`, tests
at `engine/tests/life/`, COG reference at `reference/cognition_0.4/`, and
community tooling at `tools/`.

COG-0.4, COMMUNITY-0.10, profiles, verification entry points and navigation
are separated by role. C++ 0.6 `mld` code and reports moved under
`legacy/cpp_0.6/` and `docs/history/cpp_0.6/`. Supplied reports and evidence
are historical data; they are not a local execution claim.

## Fresh checks performed

| Check | Result |
| --- | --- |
| Archive delivery manifest | 664/664 entries valid |
| `python -m unittest tools.tests.test_verify_navigation tools.tests.test_verify_cpp_0_10_import -v` | 6 tests passed |
| `python tools/verify_navigation.py` | passed |
| `python tools/verify_cpp_0_10_import.py` | passed |
| Python syntax compilation | 9 files compiled |
| Behaviour/community/COG profile JSON parse | 3 profiles valid |
| `git diff --check origin/main...HEAD` | rerun after final navigation commit |

## Native build boundary

This Windows checkout has no `cmake`, `g++`, `clang++` or `cl` command
available. Therefore CMake configuration, C++ compilation, CTest and execution
of the supplied 14 CTest targets were not freshly run here. The package's
GCC/Clang/ASan logs remain historical evidence only.
