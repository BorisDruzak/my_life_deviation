# COMMUNITY-0.10 supplied results

`release/` contains the simulation outputs supplied with the C++ 0.10.0
community archive: summaries, community state, actors, traces and command logs.
They are historical evidence for that package and its recorded Linux builds.

They do not demonstrate that this checkout was built or executed locally. New
experiments must use a distinct output directory and record their current
binary hash and profile.
