# C++ 0.6 migration verification — 2026-09-11

## Scope

This report records the migration to the active limited C++ 0.6 `mld`
laboratory core. WORLD-0.5.1-t1 is retained under `legacy/world_0.5.1-t1/`
and `docs/history/world_0.5.1-t1/`; it is not part of the active build graph.

## Source integrity

- The supplied `my_life_deviation_cpp_0.6.0_lab1.zip` hash was verified as
  `8BCABD192AA62E787B086F0847541867133923C49C741C0EEA4141C3729A7373`.
- All 78 archive files copied without transformation were compared to their ZIP
  entries by SHA-256 and matched byte-for-byte. This includes C++ sources,
  public headers, tests, the experiment runner, current mechanics, profile,
  verification scripts, reports and diagnostics.
- The supplied patch and both canonical external report files matched the
  hashes in `provenance/incoming/cpp_0.6.0_lab1/supplied-files.json`.
- The two duplicate `CPP_0.6_results` attachments and the duplicate coverage
  attachment have the same SHA-256 as their canonical counterparts; they are
  represented by provenance entries rather than redundant repository copies.

## Fresh checks run in this workspace

| Check | Result |
| --- | --- |
| Unicode-safe `python -m py_compile` over tracked Python files | passed (15 files) |
| `python -m unittest tools.tests.test_verify_navigation -v` | passed (4 tests) |
| `python tools/verify_navigation.py` | passed (`Navigation maps valid`) |
| Four relocated BEHAVIOR-0.3 verification scripts | passed through the integration test |
| CMake source-path and legacy-target static check | passed (9 CMake `.cpp` paths; no `npc_world` or `world_sim`) |
| `git diff --check origin/main...HEAD` | passed |

## Native build status

No `cmake`, `g++`, `clang++`, or `cl` command was available in this workspace
on 2026-09-11. Consequently `cmake --build` and `ctest` were not run locally.
The reports in `docs/reports/cpp_0.6/` remain archived run evidence, not a
substitute for a fresh native build. The active GitHub Actions workflow runs
the required GCC and Clang configuration, build and CTest matrix.

## Relocation correction

During verification, two archive Python scripts still resolved their inputs
relative to the former `Документация/` layout. Their path resolution was
changed to the current `docs/rules/behavior_0.3/` and
`game/profiles/behavior_0.3/` locations. The navigation test suite now runs
all four real verification scripts so this regression is detected in future.
