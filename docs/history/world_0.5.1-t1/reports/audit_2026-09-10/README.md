# Доказательства аудита 10 сентября 2026

Это реальные результаты исполнения исходного WORLD-0.5, не результаты проектируемой POLICY-0.1.

- `configure.log`, `build.log`, `build_continue.log`: конфигурация и сборка GCC14.2. Первую сборку остановил лимит инструмента; продолженная сборка завершена. Это не ошибка C++ проекта.
- `ctest.log`, `native_tests.log`: CTest2/2 и106 C++ cases,0fails.
- Четыре каталога с events/decisions/state/summary и собственными views: оригинальные20-минутные диагностические опыты.
- `fixtures/`: точные входы этих опытов.
- `probe_summary.json`: краткий первичный свод, включая первые отклонения и goal counts.
- `reproduce_original_probes.py`: переносимый runner для повторения **старого** поведения, ожидающий32rejects в прерванной встрече и отсутствие еды во время ожидания. После исправления старые ожидания должны перестать выполняться: это нормально; критерии нового поведения в61проверке docs/design.
- `fresh_verification/`: повторная проверка исходной сборки перед выдачей пакета; содержимое не подменяет первичные логи.

Полный недельный прогон в этом аудите заново не выполнялся: его анализ основан на авторских `reports/final/household_week.*`, сохранённых из исходного архива. Windows/Clang/sanitizers не перепроверялись. Все временные оценки — измерения конкретного окружения, не обещание для целевого ПК.
