# 1. Аудит документации и поведения WORLD-0.5

## 1.1. Вывод

Документация честно различает физическое ядро и экспериментальную политику. Главный разрыв — не отсутствие действий A01–A32, а отсутствие общего перехода «собственная проблема → цель → альтернативные методы → план → проверенный результат». Нельзя исправить этот разрыв единственным условием `N03 < 40 → пригласить ближайшего`.

Ниже **дефект** означает воспроизведённое нежелательное поведение или конкретное расхождение контрактов; **пробел** — не определённое решение для нового этапа; **граница** — уже честно указанное ограничение, а не обман документации. Приоритет P0 блокирует надёжную автономность, P1 нужен для законченного нового контура, P2 расширяет модель.

Ссылки на строки относятся к неизменённым файлам пользовательского архива.

## 1.2. Подтверждённые сильные стороны

`World::view` отделён от `debug_state`; цифровое получение не считается прочтением. A15 требует независимые команды всех участников одной границы, A22 прекращает будущие эффекты. Есть атомарные резервы, материальные остатки, постоянные jobs, сохранение очереди, версии договоров и детерминированные ключи наблюдений. Пройденные тесты подтверждают эти конкретные контракты, но не общую разумность поведения.

Отсутствие случайного согласия вместо решения адресата, отсутствие автоматического пополнения N03 и отсутствие самопроизвольной девиации от скуки следует сохранить.

## 1.3. Реестр находок

| ID | Приоритет / класс | Находка и точное основание | Требуемое решение |
|---|---|---|---|
| G01 | P0, граница | Бытовая политика не порождает A14 для собственного общения; `src/controller.cpp:471–565`. Недельный CSV: N03=0 у всех шести | Общая цель Need(N03), методы выбора занятия/партнёра, независимый ответ |
| G02 | P0, дефект политики | `await_response` возвращается до еды и сна, `controller.cpp:357–364,479–506` | Ожидание — неблокирующее состояние цели; безопасность проверяется перед ожиданием |
| G03 | P0, дефект политики | После прерывания A15 контроллер снова исполняет договор: проверка `completed` есть, `interrupted` не закрывает план, `controller.cpp:366–372`; отмена в `world.cpp:237–267` | Терминальный результат собственного участия закрывает SessionRuntime; повтор только новым договором |
| G04 | P0, недоопределённое правило | `terms.topic` не сверяется с каталогом/известными темами; `social.cpp:33–49`, `physical.cpp:108–141` | Закрытый каталог activity_id/topic и отдельный стабильный repetition_key |
| G05 | P0, граница | `can_decide` и `decide` запрещают работу контроллера при занятом NPC: `world.cpp:70–73`, `controller.cpp:299–302`, `app/main.cpp:132` | Разделить монитор действующего плана и выдачу новой обычной команды; A22 возможен при занятом акторе |
| G06 | P0, пробел | Прогноз — только скалярная физиология; `dynamics.cpp:105–114` всегда использует baseline и пустые эффекты | Новый профильный локальный прогноз реальных поддержанных действий; не называть старую функцию полным прогнозом |
| G07 | P0, пробел | Нет типизированной цели, её идентичности, завершения, блокировки, причины отказа; `controller.hpp:5–21` | Goal / Plan / ControllerMemory и переходы жизненного цикла |
| G08 | P1, ограничение политики | `search_width` обрезает уже построенные и отсортированные варианты, `controller.cpp:111–116` | Бюджеты генерации, поиска, маршрутов, прогнозирования и памяти, а не только длины результата |
| G09 | P1, ограничение политики | Индексы знаний используют исходную confidence и latest learned_at, без общего запроса временной применимости, полярности и конфликта: `controller.cpp:30–48` | KnowledgeQuery с true/false/unknown и отдельным описанием гипотезы о текущем состоянии |
| G10 | P1, пробел | В принятии предложения фиксированные 20/15 и `8*trust+4*affection-3`; нет сравнения с собственной альтернативой, `controller.cpp:328–354` | Собственные пороги, прогноз полного окна и упущенная полезность; accept/counter/decline |
| G11 | P1, пробел | До встречи надо одновременно прибыть и освободиться, но общего собственного календаря нет; `controller.cpp:403–419`, `action_start.cpp:500–535` | Мягкие личные интервалы, время дороги, узкое ожидание и самостоятельная готовность каждого |
| G12 | P1, пробел | Принятые, но не начатые встречи не имеют отдельного финального состояния после закрытия окна: expiry меняет только offered/countered, `economy.cpp:143–155` | SessionRuntime: missed_window / cancelled / interrupted / completed; состояние предложения не подменяет состояние исполнения |
| G13 | P1, пробел | Общей политики возврата/погашения/исполнения принятых работ нет; LIMITATIONS §2 и `controller.cpp:366–468` | Семейство FulfillCommitment; получение инструмента включает последующий возврат |
| G14 | P1, пробел | Локальный view выдаёт jobs/employment, но не отдельный безопасный индекс известных обязательств, `knowledge.cpp:339–362` | Известные условия договоров из собственной памяти + собственные receipts; без раскрытия скрытого чужого исполнения |
| G15 | P1, пробел | Decision содержит goal/score/assumptions/first, но нет разложения полезности, отсеянных вариантов и статуса исчерпания бюджета, `controller.hpp`, `controller.cpp:567–594` | Структурированный DecisionTrace для всех ветвей, включая «нет команды» |
| G16 | P1, пробел отчётов | `samples.csv` — почасовые снимки, фиксированные N01–N04; `app/main.cpp:109,117–120,150–153` | Поминутные агрегаты всех enabled needs, экспозиция дефицита и цензурированные эпизоды |
| G17 | P1, пробел отчётов | Последний sample дублируется при конце на границе часа; `app/main.cpp:150,153` | Уникальный ключ `(tick,actor)`; итоговая точка не является ещё одной минутой |
| G18 | P1, пробел отчётов | `command_rejected` и A12 decline — разные явления; публичная причина намеренно обобщена, `API.md`, `knowledge.cpp:323–338` | Отдельно social refusal, execution rejection, plan filter, timeout/cancel |
| G19 | P1, пробел приватности | Полный лог, авторская причина отказа и сформулированное объяснение собеседнику имеют разные права | Не соединять эти уровни в NPC View; communicated reason — сообщение, не «истинная причина» |
| G20 | P1, пробел воспроизведения | Сегодня политика почти без собственного persistent state; snapshot хранит мир, а CLI-сценарий передаётся отдельно | Снимок SimulationCheckpoint включает policy_version/hash, память целей, таймеры, курсоры и связи команд |
| G21 | P1, неполное действие | Все варианты A15 физически остаются общением с базовым удовольствием; слово sport не добавляет спорт/мяч/нагрузку, `physical.cpp:102–145` | Разделить тему речи и контракт занятия; реальный спорт/чтение требуют ресурсов и собственных эффектов |
| G22 | P1, ограничение политики | Один старый запрос предмета делает `addressed=true` независимо от его давности/новой ситуации, `controller.cpp:211–216` | Конечные cooldown и пересмотр по новым локальным сведениям вместо вечного запрета |
| G23 | P1, пробел измерений | Отсутствие социальных событий не объясняет, что именно отсутствовало: цель, знания, контакт, время, согласие или ресурсы | Воронка goal → candidate → offer → read → answer → attend → start → effect |
| G24 | P2, явно отложено | Нет доказанного поведения на 1000 NPC, компактирования знания, общей логики угроз/романтики/репутации | Не объявлять это закрытым новыми тремя подсистемами; отдельные эксперименты и спецификации |

## 1.4. Дополнительные воспроизводимые опыты

Все четыре сценария находятся в `reports/audit_2026-09-10/fixtures/`; применялись к исходному executable, без исправления кода. Длительность каждого — 20 игровых минут.

### RPROBE-01: нормальное завершение — контроль против ложного обвинения

На t=0 организатор предлагает встречу, на t=1 второй отвечает, на t=2 оба контроллера подают A15. Продолжительность — 3 минуты. Получено ровно одно `joint_started`, одно `joint_ended`, ноль отклонённых команд. На t=5 оба выбирают idle. Предположение о повторе **любого** завершённого договора не подтвердилось; в реестр такой дефект не включён.

### RPROBE-02: прерывание встречи

Та же постановка, но на t=3 участник b отзывает участие A22. Прошедшая одна минута совместности сохраняется. На t=4…19 оба контроллера снова выдают A15 по той же версии; суммарно **32 command_rejected**, а намерение `attend_agreement` записано 34 раза, включая два первоначальных старта. Источник: `interrupted_joint_retry/summary.json`, `decisions.jsonl`, `events.jsonl`.

Причина: локальная политика игнорирует собственный `interrupted` receipt и полученный факт остановки при выборе договора. Исполнитель правильно отвергает повтор — ломается не согласие, а управление намерением.

### RPROBE-03: ожидание важнее доступной еды

Начальная N01 у a — 15.5, critical=15, готовая порция из 15 единиц лежит в его инвентаре. Он отправляет предложение пассивному b. За t=1…19 записано 19 решений `await_response`; еды не употреблено, все 15 единиц остались. На t=20 N01=**14.433333333333344**, возникает `need_critical`.

При текущем расходе 3.2/час это соответствует `15.5 − 20*3.2/60`. Проблема не в отсутствии ресурсов и не в формуле потребности, а в блокирующем раннем return.

### RPROBE-04: произвольная тема как физический ключ

В terms установлено `topic="unregistered_topic_for_audit"`. Предложение, согласие и A15 успешно исполнены, создан отдельный ключ repetition. У обоих на t=20 N03=70.38560352228237 вместо исходных 70 с обычным расходом.

Это доказывает принятие незарегистрированной темы и связь строки с эффектом, но не доказывает, что действующая household-политика уже эксплуатирует бесконечную смену строк: она вообще не генерирует A14. Возможность обхода привыкания — следствие текущего контракта и должна быть закрыта до общего генератора.

## 1.5. Что видно в приложенном недельном опыте

`reports/final/household_week.summary.json`: 6 NPC, t=360…10440, 101 технический отказ. Разбивка: 67 недоступных товаров, 34 занятых ресурсов. `household_week.samples.csv`: 1020 строк, включая повтор финальной точки по каждому актору; 169 уникальных часовых границ × 6 = 1014 уникальных `(tick,actor)`.

Первое **зафиксированное почасовым снимком** нулевое N03 — t=4020 у всех шести, то есть через 3660 минут / 61 час от старта. Точная минута достижения нуля по этим CSV не определяется. Нельзя вычислять точную длительность критического состояния и причинную воронку по редким снимкам.

## 1.6. Что не следует «исправлять»

Не заставлять адресата соглашаться ради зелёного теста. Не наказывать доверие за обычный отказ. Не давать N03 от предложения, принятия или совместного местоположения без активности. Не разрешать будущий план резервировать чужое тело/деньги. Не выдавать private debug_reason в локальный view. Не приписывать недостижимой цели «нежелание», когда закончился бюджет поиска.

## 1.7. Предлагаемые изменения действующей документации при реализации

`WORLD_0.5.md` не переписывать задним числом. Создать версионированный новый нормативный документ; прежние запуски воспроизводятся по 0.5. В ERRATA добавить G02/G03 как исправления политики, G04/G21 — как ужесточение/расширение контракта занятия. В API описать состояние контроллера и версии отчётов; в PARAMETERS отделить world coefficients от policy tuning. ACCEPTANCE дополнить сквозными социальными сценариями; LIMITATIONS уменьшать только после соответствующих зелёных проверок.
